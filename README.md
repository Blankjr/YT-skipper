# There are way better ways, but someone asked for exactly this type of prototype

# Installation

1. pip install pyautogui

    - sudo pacman -S scrot
    - sudo pacman -S tk

2. pip install opencv-python

3. pip install pillow
